Adding onto version1.0 this will now have a username and password box.  This will store the username and password internally, while at the 
same time hiding the password in plain sight via stars(*)

The enter button is there for dropbox API
